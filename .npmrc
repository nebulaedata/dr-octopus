# npm 发布配置说明
#
# 本仓库的发布单元是 release/ 目录（pnpm release:build 的产出），
# 包名 dr-octopus（见 release.config.json）。
# npm publish 在 release/ 内执行时，只读取以下位置的配置：
#   release/.npmrc（项目级） -> ~/.npmrc（用户级） -> 环境变量
# 因此仓库根本文件不会被发布命令读取，请勿在此存放任何认证信息。
#
# 本地发布（推荐，token 写入 ~/.npmrc，不会进入仓库）：
#   npm config set //registry.npmjs.org/:_authToken <token> --location=user   # 首次使用，仅一次
#   或交互登录（2FA 账号每次发布需加 --otp）：npm login --registry=https://registry.npmjs.org
#   pnpm release:publish
# 一条命令完成：检测登录 →（版本已占用则自动 release-it 升版本）→ 构建 → 确认后发布。
# 可用参数：--bump（强制升版本）、--dry-run（试跑不上传）、-y（跳过确认）、--otp <code>（2FA）、--skip-build。
# 切勿把 token 写入本文件：本文件被 git 跟踪会泄露；且 npm publish 在 release/ 内执行，
#   只读取 release/.npmrc 与 ~/.npmrc，根 .npmrc 中的 token 对发布不生效。
#
# CI 发布（NPM_TOKEN 为 CI 的 secret 环境变量）：
#   cd release
#   npm config set //registry.npmjs.org/:_authToken "$NPM_TOKEN" --location=user
#   npm publish
#
# 国内加速：如需 npmmirror 加速依赖安装，取消下行注释。
# 注意 npmmirror 不支持发包，发布时必须显式指定官方源：
#   npm publish --registry=https://registry.npmjs.org
# registry=https://registry.npmmirror.com
