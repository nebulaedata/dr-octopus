# 定时任务通知闭环端到端验证

验证时间：2026-09-06 23:52 至 2026-09-07 00:01（Asia/Shanghai）。

环境：Windows 本地开发服务，Web `http://localhost:5173`，独立 Scheduler daemon，真实 DeepSeek V4 Flash 模型。通过浏览器操作会话、通知、结果及历史；持续授权、排期和归档恢复删除使用公开 HTTP 接口。本次不是完全由浏览器点击完成的自动化测试。

## 测试身份

- 来源会话：`7968e467-ce6a-4d34-8d29-2fde1e338067`。
- 任务：`31b6b22e-9f88-4f10-a2e3-26491e23a2b2`，名称 `通知闭环 E2E 20260906-2353`。
- 成功运行：`4f36241a-3852-4fc6-98e7-c3e11bcf0b06`。
- 权限不足运行：`4ceab8b1-76bc-4795-800a-69bd25ec0564`。

## 实测结果

| 环节           | 操作与证据                                                                                              | 结果                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| 会话创建       | 在真实 Composer 发送请求，模型调用 `scheduler_create`，通过单次权限确认；创建禁用任务并正确关联来源会话 | 通过                                                                  |
| 未授权防护     | 未授权调用立即运行；没有创建执行记录                                                                    | 通过，错误映射修复后返回 403、`retryable:false`                       |
| 持续授权       | 预览当前工具身份，只授权 `bash`；修改排期、启用时保留授权                                               | 通过                                                                  |
| 真正定时执行   | 计划 23:55:03.321，23:55:17.810 开始，23:55:20.843 结算；`triggerSource=schedule`                       | 成功，输出 `NOTIFY_E2E_20260906_2353`                                 |
| 自动通知       | 停留任务页面，自动生成 `scheduled-<runId>` 结果会话；来源与结果均出现未读蓝点，通知数 2→3               | 通过                                                                  |
| 已读持久化     | 从通知打开结果后蓝点消失，通知数 3→2；结果的 `notificationVersion=readVersion=174`                      | 通过                                                                  |
| 来源摘要       | 来源会话显示 Scheduler 摘要及可展开的结果链接，链接指向同一结果会话；来源已读版本推进至 174             | 通过                                                                  |
| 只读执行结果   | 展示原始指令、实际 bash 调用、工具输出和最终回答；无 Composer，结果 Session 无 Runtime                  | 通过                                                                  |
| 归档与恢复     | 归档后仍能解析原结果会话；恢复后暂停、授权引用清空、需重新授权，立即运行返回 403                        | 通过                                                                  |
| 权限不足分支   | 恢复后只授权 `read`，手动执行实际需要 `bash` 的任务                                                     | 正确结算 `needs_attention` / `SCHEDULE_PERMISSION_DENIED`，工具被中止 |
| 失败通知与历史 | 自动生成第二个结果会话及未读通知；运行记录 Dialog 同时包含成功与失败，支持展开执行详情、打开结果会话    | 通过                                                                  |
| 彻底删除       | 仅对本次任务归档后 purge；两个结果会话及任务通知均清理，来源会话保留                                    | 通过                                                                  |
| 测试清理       | 随后删除本次来源测试会话及其文件；原有 7 个归档任务和未归档任务列表与测试前 JSON 完全一致               | 通过                                                                  |
| 服务收尾       | daemon 保持 control-ready、active，queued=0、running=0、nextRunAt=null                                  | 通过                                                                  |

## 本次发现并修复

1. Server 将 `SCHEDULE_AUTHORIZATION_REQUIRED` 当作 503 基础设施故障，隐藏业务提示并标记可重试。现映射为 403，保留“先授权再运行”提示；真实恢复后立即运行请求已复测。新增回归同时确保 `SCHEDULE_AUTHORIZATION_UNAVAILABLE` 仍为 503。
2. 长结果会话标题与顶部导航重叠。导航改为保持自身宽度，标题使用剩余空间截断；浏览器截图复核无重叠。

## 检查与范围

- Server：build、lint、typecheck 通过；完整测试 205 项通过。
- Web：lint、测试、build 通过；build 内含 `tsc -b` 类型检查（此包没有独立 typecheck 脚本）。
- 本轮未修改 Agent；此前 Agent 154 项回归已通过，本轮使用真实 daemon 完成成功和权限不足两次执行。
- 本次未重新覆盖取消中断、系统休眠唤醒、跨平台、超大历史分页，以及持续授权按钮的完整浏览器点击交互。此处不将这些项目记为本轮已测。

