/**
 * @author Codex
 * @description Keeps queued job and produced artifact identities consistent as document extraction contracts evolve.
 */
export const ATTACHMENT_PROCESSOR = { id: 'attachment-processor', version: '1.4.0' } as const;
