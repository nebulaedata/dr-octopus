/**
 * @author Codex
 * @description Defines Session Composer presentation modes shared by its feature controls.
 */

/**
 * Selects the browser-side Agent interaction workflow.
 */
export type AgentWorkMode = 'agent' | 'plan' | 'knowledge';

export type { PermissionMode } from '@octopus/shared/protocol';
